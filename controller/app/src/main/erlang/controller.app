{application, ${ARTIFACT},
[{description, ${DESCRIPTION}},
{id, ${ID}},
   {vsn, ${VERSION}},
   {modules, ${MODULES}},
   {registered, ${REGISTERED}},
   {mod, {'controller_app', []}},
   {applications, ${APPLICATIONS}}]}.
