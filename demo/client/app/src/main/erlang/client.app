{application, ${ARTIFACT},
[{description, ${DESCRIPTION}},
{id, ${ID}},
   {vsn, ${VERSION}},
   {modules, ${MODULES}},
   {registered, ${REGISTERED}},
   {mod, {'client_app', []}},
   {applications, ${APPLICATIONS}}]}.
