{application, ${ARTIFACT},
[{description, ${DESCRIPTION}},
{id, ${ID}},
   {vsn, ${VERSION}},
   {modules, ${MODULES}},
   {registered, ${REGISTERED}},
   {mod, {'server_app', []}},
   {applications, ${APPLICATIONS}}]}.
