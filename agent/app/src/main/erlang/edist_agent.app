{application, ${ARTIFACT},
[{description, ${DESCRIPTION}},
{id, ${ID}},
   {vsn, ${VERSION}},
   {modules, ${MODULES}},
   {registered, ${REGISTERED}},
   {mod, {'edist_agent_app', []}},
   {applications, ${APPLICATIONS}}]}.
